name: ssq-report

on:
  schedule:
    - cron: '0 2 * * 1,3,5'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run
        run: python ssq_report.py
